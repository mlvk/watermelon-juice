import { currency } from 'watermelon-juice/helpers/currency';
import { module, test } from 'qunit';

module('Unit | Helper | currency');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = currency([42]);
  assert.ok(result);
});
